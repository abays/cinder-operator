---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: cinders.cinder.openstack.org
spec:
  group: cinder.openstack.org
  names:
    kind: Cinder
    listKind: CinderList
    plural: cinders
    singular: cinder
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Cinder is the Schema for the cinders API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CinderSpec defines the desired state of Cinder
            properties:
              cinderAPIContainerImage:
                description: CinderAPIContainerImage - Cinder API Container Image
                  URL
                type: string
              cinderAPIReplicas:
                description: CinderAPIReplicas - Cinder API Replicas
                format: int32
                type: integer
              cinderApiNodeSelector:
                additionalProperties:
                  type: string
                description: CinderAPINodeSelector to target subset of worker nodes
                  for running the API service
                type: object
              cinderBackupContainerImage:
                description: CinderBackupContainerImage - Cinder Backup Container
                  Image URL
                type: string
              cinderBackupNodeSelector:
                additionalProperties:
                  type: string
                description: CinderBackupNodeSelector to target subset of worker nodes
                  for running the Backup service
                type: object
              cinderBackupReplicas:
                description: CinderBackupReplicas - Cinder Backup Replicas
                format: int32
                type: integer
              cinderSchedulerContainerImage:
                description: CinderSchedulerContainerImage - Cinder Scheduler Container
                  Image URL
                type: string
              cinderSchedulerNodeSelector:
                additionalProperties:
                  type: string
                description: CinderSchedulerNodeSelector to target subset of worker
                  nodes for running the Scheduler service
                type: object
              cinderSchedulerReplicas:
                description: CinderSchedulerReplicas - Cinder Scheduler Replicas
                format: int32
                type: integer
              cinderVolumes:
                description: CinderVolumes - cells to create
                items:
                  description: Volume defines cinder volume configuration parameters
                  properties:
                    containerImage:
                      description: Cinder Volume Container Image URL
                      type: string
                    customServiceConfig:
                      default: '# add your customization here'
                      description: CustomServiceConfig - customize the service config
                        using this parameter to change service defaults, or overwrite
                        rendered information using raw OpenStack config format. The
                        content gets added to to /etc/<service>/<service>.conf.d directory
                        as custom.conf file.
                      type: string
                    name:
                      description: Name of cinder volume service
                      type: string
                    nodeSelector:
                      additionalProperties:
                        type: string
                      description: Cinder Volume node selector
                      type: object
                    replicas:
                      default: 1
                      description: Cinder Volume Replicas
                      format: int32
                      type: integer
                  required:
                  - replicas
                  type: object
                type: array
              customServiceConfig:
                default: '# add your customization here'
                description: CustomServiceConfig - customize the service config using
                  this parameter to change service defaults, or overwrite rendered
                  information using raw OpenStack config format. The content gets
                  added to to /etc/<service>/<service>.conf.d directory as custom.conf
                  file.
                type: string
              databaseInstance:
                description: MariaDB instance name Right now required by the maridb-operator
                  to get the credentials from the instance to create the DB Might
                  not be required in future
                type: string
              databaseUser:
                default: cinder
                description: 'DatabaseUser - optional username used for cinder DB,
                  defaults to cinder TODO: -> implement needs work in mariadb-operator,
                  right now only cinder'
                type: string
              debug:
                description: Debug - enable debug for different deploy stages. If
                  an init container is used, it runs and the actual action pod gets
                  started with sleep infinity
                properties:
                  bootstrap:
                    default: false
                    description: Bootstrap enable debug
                    type: boolean
                  dbSync:
                    default: false
                    description: DBSync enable debug
                    type: boolean
                  service:
                    default: false
                    description: Service enable debug
                    type: boolean
                type: object
              defaultConfigOverwrite:
                additionalProperties:
                  type: string
                description: 'ConfigOverwrite - interface to overwrite default config
                  files like e.g. logging.conf or policy.json. But can also be used
                  to add additional files. Those get added to the service config dir
                  in /etc/<service> . TODO: -> implement'
                type: object
              passwordSelectors:
                description: PasswordSelectors - Selectors to identify the DB and
                  AdminUser password and TransportURL from the Secret
                properties:
                  admin:
                    default: CinderPassword
                    description: Database - Selector to get the cinder service password
                      from the Secret
                    type: string
                  database:
                    default: CinderDatabasePassword
                    description: 'Database - Selector to get the cinder database user
                      password from the Secret TODO: not used, need change in mariadb-operator'
                    type: string
                  transportUrl:
                    default: TransportURL
                    description: Database - Selector to get the cinder service password
                      from the Secret
                    type: string
                type: object
              preserveJobs:
                default: false
                description: PreserveJobs - do not delete jobs after they finished
                  e.g. to check logs
                type: boolean
              secret:
                description: Secret containing OpenStack password information for
                  CinderDatabasePassword, AdminPassword
                type: string
              serviceUser:
                default: cinder
                description: ServiceUser - optional username used for this service
                  to register in cinder
                type: string
            required:
            - cinderAPIReplicas
            - cinderBackupReplicas
            - cinderSchedulerReplicas
            type: object
          status:
            description: CinderStatus defines the observed state of Cinder
            properties:
              conditions:
                description: Conditions
                items:
                  description: Condition - A particular overall condition of a certain
                    resource
                  properties:
                    lastHearbeatTime:
                      format: date-time
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      description: Reason - Why a particular condition is true, false
                        or unknown
                      type: string
                    status:
                      type: string
                    type:
                      description: Type - A summarizing name for a given condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              databaseHostname:
                description: Cinder Database Hostname
                type: string
              hash:
                additionalProperties:
                  type: string
                description: Map of hashes to track e.g. job status
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
